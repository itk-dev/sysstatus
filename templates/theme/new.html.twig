{% extends '@EasyAdmin/default/layout.html.twig' %}

{% block page_title %}
  {{ ('theme.new.headline')|trans }}: {{ theme.name }} (#{{ theme.id }})
{% endblock %}

{% block content_title %}
  {{ ('theme.new.headline')|trans }}: {{ theme.name }} (#{{ theme.id }})
{% endblock %}

{% block main %}
  {{ include('theme/_form.html.twig', {'button_label': ('common.actions.save'|trans)}) }}
{% endblock %}

{% block body_javascript %}
  <script type="text/javascript" src="/form_theme.js"></script>

  <script type="text/javascript">
    $(document).ready(function() {
      $('#theme_systemGroups').select2();
      $('#theme_reportGroups').select2();
    });
  </script>
{% endblock %}
